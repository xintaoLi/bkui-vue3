import VxeColumnComponent from '../main/column';
import { withInstall } from '@bkui-vue/shared';

const component = withInstall(VxeColumnComponent);
export const Column = component;
export default component;
